# Publishing Notes

Recommended public repository:

```text
huzheyu93-afk/lunq-compute-delivery-sdk
```

Recommended description:

```text
Public-safe verifier and schema starter for LUNQ compute delivery receipts.
```

Recommended topics:

```text
lunq, compute-delivery, proof-of-compute, gpu, ai-compute, verification, delivery-receipt
```

Launch checklist:

- `npm run check` passes
- repository is public
- MIT license detected
- no production API URLs, credentials, customer data, wallets, liquidity, KYC, or compliance vendor configuration
- README keeps the project framed as verified compute delivery, not investment, yield, futures, or guaranteed rewards
